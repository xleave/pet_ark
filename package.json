{
  "name": "pet-ark-all-characters",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Auditable, code-drawn Arknights character collection for the Codex pet atlas format.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "build:all": "node scripts/build.mjs --all",
    "validate": "node scripts/validate.mjs",
    "validate:all": "node scripts/validate.mjs --all",
    "test": "node scripts/test-registry.mjs",
    "rebuild": "npm run build && npm run validate"
  },
  "dependencies": {
    "sharp": "^0.34.3"
  }
}
